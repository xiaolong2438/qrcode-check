{
  "name": "wechat-cleaner-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "db:init": "wrangler d1 execute cleaner_check_db --local --file=./schema.sql --persist-to .wrangler/state",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  }
}
